import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'User-App-Lottired-Pos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
